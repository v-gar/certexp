#ifndef _CONSTANTS_H
#define _CONSTANTS_H

//! Maximum length of a hostname
const unsigned int MAX_HOSTNAME_LENGTH = 256;

//! Simple regex for rudimentary validation
const char *DOMAIN_REGEX = "[0-9a-z\\.\\-]+";

#endif
